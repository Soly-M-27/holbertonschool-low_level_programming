#include "main.h"

/**
 * mul - Function that multiplies two integers
 * @a: Holds one of the numbers to multiply
 * @b: Holds another of the numbers to multiply
 *
 * Return: mul
 */

int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
