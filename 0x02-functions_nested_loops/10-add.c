#include "main.h"

/**
 * add - Function that adds two integers and returns the result
 * @a: hold number in a
 * @b: holds number in b
 *
 * Return: result
 */

int add(int a, int b)
{
	int total = a + b;

	return (total);
}
