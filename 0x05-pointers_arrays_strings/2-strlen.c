#include "main.h"
#include <string.h>

/**
 * _strlen = function that returns the length of a string
 * @s: Holds length
 *
 * Return: length
 */

int _strlen(char *s)
{
	int len;
	len = strlen(s);

	return (len);
}
