#include "main.h"
#include <string.h>

/**
 * _puts - function that prints a string, followed by a new line, to stdout
 * @str: Holds the string
 *
 * Return: void
 */

void _puts(char *str)
{
	puts(str);
}
